{
  "name": "@phone_catalog/monorepo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "vite": "^2.8.6"
  },
  "devDependencies": {
    "eslint": "^8.11.0",
    "prettier": "^2.6.1",
    "typescript": "^4.6.3",
    "vitest": "^0.7.11"
  },
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build:server": "pnpm --filter fastify-backend run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Leonardo Escuer Ruiz",
  "license": "ISC"
}
